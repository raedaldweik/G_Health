/*
 * Nabd target architecture on Google Cloud — the content behind the Architecture tab.
 * Written as a deployable design, not a demo diagram: every node carries why / scale / alternative /
 * phase-1 equivalent / cost so the interviewer can click anything and get a principal-architect answer.
 * Facts (regions, prices, product names) verified 2026-09-07 against the Google Cloud price/location pages.
 */

export const LANES = [
  { id: 'sources', title: 'Sources', sub: 'What Qatar already has' },
  { id: 'ingest', title: 'Ingest & harmonise', sub: 'HL7v2 · FHIR · files · PDFs' },
  { id: 'store', title: 'Store & govern', sub: 'me-central1 (Doha) · PHI never leaves' },
  { id: 'ml', title: 'Analytics & ML', sub: 'BigQuery · Vertex' },
  { id: 'agents', title: 'Agents', sub: 'ADK on Cloud Run (Doha) · Gemini · MCP' },
  { id: 'exp', title: 'Experience', sub: 'clinicians · ministry' },
];

export const NODES = [
  // ── Sources ──
  { id: 'qhie', lane: 0, row: 0, label: 'QHIE — national HIE', sub: 'FHIR R4 · Patient · Observation · MedicationRequest · Encounter', icon: 'hie',
    why: 'The system of record already exists. Nabd does not replace it; it subscribes to it. Everything downstream is derived, rebuildable, and traceable to a QHIE resource id.',
    scale: '~3M citizens and residents; Qatar records ~30k encounters/day, roughly 500k FHIR resource updates/day with a 20/s lunchtime peak.',
    alt: 'Direct database access to HMC/PHCC systems — rejected: bypasses the national consent model and creates 18 point-to-point integrations.',
    p1: 'Synthetic HIE (8 relational tables, 36 months, LOINC/ICD-10/ATC coded) generated by a seeded script; FHIR R4 NDJSON sample exported for shape-compatibility.',
    cost: 'None to Google Cloud — the QHIE side pays the FHIR subscription egress.' },
  { id: 'ehr', lane: 0, row: 1, label: 'HMC · PHCC EHRs', sub: 'HL7v2 ADT / ORU / RDE over MLLP', icon: 'hospital',
    why: 'Facilities that are not yet on QHIE\'s FHIR feed still emit HL7v2. Meeting them where they are is the difference between 4 pilot sites and 18.',
    scale: 'Peak ~40 HL7v2 messages/s nationally; each ~4 KB.',
    alt: 'Wait for full FHIR adoption — rejected; it delays the population view by years for the facilities that need it most (the flagged ones on the Geography tab).',
    p1: 'Not simulated; the synthetic generator emits already-harmonised tables.',
    cost: 'Site VPN / Interconnect to me-central1 (≈ $300–900/mo per Cloud VPN gateway pair, national).' },
  { id: 'claims', lane: 0, row: 2, label: 'Claims · pharmacy · registries', sub: 'nightly batch · CSV / X12', icon: 'files',
    why: 'Cost and dispensing data arrive nightly and drive the Cost & Equity story. There is no clinical reason to stream them.',
    scale: '~1.5M rows/night.',
    alt: 'Streaming claims — over-engineering; the consumer (monthly cost concentration) is a batch report.',
    p1: 'claims.csv.gz and dispensing tables in the synthetic HIE.',
    cost: 'Negligible (Cloud Storage ingest is free; storage ≈ $0.02/GB-mo).' },
  { id: 'guidelines', lane: 0, row: 3, label: 'MOPH clinical guidelines', sub: 'PDF · protocols · formulary', icon: 'doc',
    why: 'The agent is only allowed to make a clinical claim with a citation. The corpus is the source of that trust.',
    scale: 'Dozens of documents, thousands of pages; changes quarterly.',
    alt: 'Web search grounding — rejected for clinical content: unbounded, un-versioned, not MOPH-approved.',
    p1: 'MOPH PDFs chunked, BM25 + gemini-embedding-001 hybrid index with page-level citations.',
    cost: 'One-off embedding ≈ $0.15 per 1M tokens; a full re-embed of the corpus is cents.' },

  // ── Ingest ──
  { id: 'fhirstore', lane: 1, row: 0, label: 'Cloud Healthcare API — FHIR store', sub: 'validated mirror · versioning · consent · Pub/Sub notifications', icon: 'fhir',
    why: 'A managed FHIR R4 store gives schema validation, resource history, consent enforcement and a native streaming export to BigQuery. It is the healthcare-grade landing zone; BigQuery alone is not.',
    scale: 'Managed, regional — available in me-central1 (Doha) and me-central2. Streaming export to BigQuery is per-resource and "near real time" (Google publishes no latency SLA; seconds in practice); max 10 stream configs per store; bulk imports need a one-off batch export.',
    alt: 'FHIR NDJSON → Cloud Storage → Dataflow → BigQuery — only if MOPH already operates a FHIR server it wants to keep as the mirror; you lose consent enforcement and notifications.',
    p1: 'pandas tables in-process with a FHIR-shaped export; consent enforced in the tool layer (CONSENT·DENY audited).',
    cost: 'Doha list: structured storage $0.292/GiB-mo, requests $0.39 per 100k, notifications $0.29 per 1M, streaming export $0.34→$0.24/GiB. ≈ 1.2 TB + 5M requests ≈ $400–700/mo national.' },
  { id: 'mllp', lane: 1, row: 1, label: 'MLLP → HL7v2 store → Dataflow', sub: 'GKE Autopilot · Whistle mappings to FHIR', icon: 'transform',
    why: 'Google\'s open-source MLLP adapter terminates the hospital connection on GKE; the HL7v2 store parses and the harmonisation pipeline maps to FHIR. Standard, documented, supportable.',
    scale: 'Adapter pods scale per site; Dataflow streaming autoscaled by backlog.',
    alt: 'Commercial integration engine (Rhapsody / Mirth) — valid if MOPH already licenses one; then it feeds the FHIR store directly. Note: Google\'s reference Dataflow harmonisation repo was archived in 2025, so the Beam/Whistle pipeline is code we own and test.',
    p1: 'n/a — synthetic data is born harmonised.',
    cost: 'GKE Autopilot pods ≈ $150/mo; Dataflow streaming ≈ $400–800/mo national.' },
  { id: 'batchland', lane: 1, row: 2, label: 'GCS landing → Dataflow batch', sub: 'schema checks · dedupe · quarantine', icon: 'bucket',
    why: 'Nightly files need validation and idempotent loads. Dataflow batch is cheap, restartable and leaves a quarantine bucket for bad rows.',
    scale: '1.5M rows in ~6 minutes on 4 workers.',
    alt: 'BigQuery LOAD directly — fine for clean files; keep Dataflow only where mapping is needed.',
    p1: 'Generator writes clean tables directly.',
    cost: '≈ $30–80/mo.' },
  { id: 'docai', lane: 1, row: 3, label: 'Document pipeline', sub: 'chunk · embed · version the corpus', icon: 'embed',
    why: 'Guideline updates must be reproducible: a versioned corpus, a fixed chunker, embeddings tied to a model version. Otherwise citations rot.',
    scale: 'Event-driven (Cloud Run job on bucket upload).',
    alt: 'Manual re-indexing — how corpora go stale.',
    p1: 'rag.py builds the hybrid index at startup and caches embeddings.',
    cost: 'Cents per re-index.' },
  { id: 'pubsub', lane: 1, row: 4, label: 'Pub/Sub — change events', sub: 'FHIR resource notifications · fan-out', icon: 'pubsub',
    why: 'Decouples producers from consumers. A new HbA1c result fans out to the scorer, the care-gap engine and the audit sink without any of them knowing each other.',
    scale: 'Effectively unlimited; ordering keys per patient keep event order.',
    alt: 'Polling the FHIR store — wasteful and late.',
    p1: 'Not needed in-process.',
    cost: '≈ $40 per TiB delivered; national volume is a few GB/day → tens of dollars.' },

  // ── Store & govern ──
  { id: 'bq', lane: 2, row: 0, label: 'BigQuery — analytics lakehouse', sub: 'FHIR streaming export · curated marts · partitioned & clustered', icon: 'bq',
    why: 'Population health is a set-based problem: cohorts, rates, trends across 3M people. BigQuery answers those in seconds and is the substrate for BigQuery ML, Looker, and the MCP cohort tools.',
    scale: 'Serverless; the FHIR tables grow ≈ 2 TB/year. Partition by date, cluster by patient_id, use BI Engine for dashboard hot data.',
    alt: 'AlloyDB for analytics — wrong tool; row store, manual scaling. Spanner — over-engineered for read-mostly analytics.',
    p1: 'pandas in memory (4,000 patients, 8 tables).',
    cost: 'Doha list: $6.25/TiB on-demand, active storage $0.026/GiB-mo, Standard edition $0.04/slot-h. 3 TB + 20 TiB scanned ≈ $200/mo; ≈ $1k/mo with dashboards and BI Engine.' },
  { id: 'deid', lane: 2, row: 1, label: 'De-identification · consent', sub: 'Healthcare API de-id · consentConfig.accessEnforced · pseudonymous zone', icon: 'shield',
    why: 'The agents and the LLM operate on a pseudonymised analytics zone: stable QH-ids, no names, no national ids. Re-identification happens only in the app layer, inside the perimeter, for an authorised clinician. Consent is enforced by the store, not by prompt.',
    scale: 'Managed API; runs as a pipeline stage on every load.',
    alt: 'Trusting the prompt to "not reveal PII" — this is the definition of AI garbage.',
    p1: 'Patients carry pseudonymous QH-ids; restricted patients return CONSENT DENIED from every tool.',
    cost: 'De-id per-GB processed; small.' },
  { id: 'fs', lane: 2, row: 2, label: 'Vertex AI Feature Store', sub: 'online features · point-in-time correct offline', icon: 'features',
    why: 'The same 30 features the model trained on must be served in milliseconds at event time. Feature Store gives online serving from a BigQuery source and removes training/serving skew.',
    scale: 'Bigtable online serving (the only online store going forward — Optimized serving sunsets Feb 2027); 3M entities is small. Available in Doha.',
    alt: 'Compute features in the scorer — training/serving skew, the #1 silent ML failure.',
    p1: 'Features computed by the same function at train and score time (hie.py).',
    cost: 'Doha Bigtable node $1.14/h ≈ $830/mo for one node; pilot reads features straight from BigQuery instead.' },
  { id: 'alloydb', lane: 2, row: 3, label: 'AlloyDB — application state', sub: 'HITL queue · audit trail · drafts · users', icon: 'db',
    why: 'The queue and audit trail are transactional, low-volume and must be durable. PostgreSQL-compatible, HA, columnar engine for the audit reports.',
    scale: 'One primary + read pool; trivial load.',
    alt: 'Firestore — fine too; AlloyDB chosen for SQL audit queries and MOPH DBA familiarity.',
    p1: 'JSON files on disk (queue_service.py, audit.py).',
    cost: '≈ $250/mo basic → ≈ $900/mo HA national.' },
  { id: 'vector', lane: 2, row: 4, label: 'Vertex AI RAG Engine', sub: 'guideline corpus · grounding metadata · page-span citations', icon: 'rag',
    why: 'Managed retrieval with citations and corpus versioning, callable as a Gemini tool. Replaces the hand-built hybrid index with the same contract.',
    scale: 'Managed vector DB; corpus is tiny.',
    alt: 'Vertex AI Search — heavier, built for web/enterprise search; Agent Search for healthcare is being retired in 2027.',
    p1: 'BM25 + gemini-embedding-001 hybrid in rag.py with the same citation shape.',
    cost: 'RagManagedDb basic tier ≈ $60–120/mo.' },
  { id: 'catalog', lane: 2, row: 5, label: 'Dataplex — catalog · lineage · DQ', sub: 'data products · owners · quality rules', icon: 'catalog',
    why: 'A ministry will ask "where did this number come from?" Lineage from QHIE resource → BigQuery mart → model feature → agent answer must be automatic.',
    scale: 'Metadata only.',
    alt: 'A wiki. No.',
    p1: 'Data tab documents every table and the generator; audit trail records every tool call.',
    cost: 'Low (metadata storage + DQ scan compute).' },

  // ── Analytics & ML ──
  { id: 'train', lane: 3, row: 0, label: 'Vertex AI Pipelines — training', sub: 'XGBoost risk · KMeans segments · Ridge forecast · scheduled', icon: 'pipeline',
    why: 'Retraining is a pipeline, not a notebook: data snapshot → features → train → held-out eval → fairness gate → register. Every run reproducible.',
    scale: 'Weekly schedule + drift-triggered; CPU is enough for tabular XGBoost on 3M rows (minutes).',
    alt: 'BigQuery ML for the first-cut model — excellent for a baseline; export to Vertex for the eval gate and serving.',
    p1: 'scripts/train_models.py at container build: 75/25 stratified split, model cards, held-out predictions persisted for the Evaluation tab.',
    cost: '$0.03 per pipeline run + compute; ≈ $50–300/mo.' },
  { id: 'registry', lane: 3, row: 1, label: 'Model Registry', sub: 'versions · model cards · approval · lineage', icon: 'registry',
    why: 'The model behind a clinical alert must be a named, approved version. Registry ties model → dataset → pipeline run → card.',
    scale: 'n/a',
    alt: 'Files in a bucket — no approvals, no lineage.',
    p1: 'models/ directory with model_cards.json and pinned artefacts.',
    cost: 'Free.' },
  { id: 'endpoint', lane: 3, row: 2, label: 'Online endpoint — risk scoring', sub: 'Vertex AI Prediction · autoscaled · p50 < 10 ms', icon: 'endpoint',
    why: 'An HbA1c result must update a patient\'s risk while the result is still fresh. Real-time scoring is per-patient and cheap (a 300-tree XGBoost scores in ~1 ms of CPU).',
    scale: '2 min replicas for HA, scale on CPU; a single e2-standard-4 replica sustains >1,000 scores/s — national peak is ~20/s.',
    alt: 'Score in the agent process — no versioning, no canary. BigQuery ML batch scoring alone — right for the nightly cohort (1–2 s latency), wrong for the point-of-care event.',
    p1: 'ml.py scores in-process with pred_contribs for explanations; same feature function.',
    cost: 'Doha e2-standard-4 ≈ $0.187/h → 2 replicas 24/7 ≈ $275/mo; 4 replicas ≈ $550.' },
  { id: 'batchpred', lane: 3, row: 3, label: 'Nightly batch scoring', sub: 'whole cohort re-scored 02:00 AST → BigQuery', icon: 'batch',
    why: 'The ministry view (bands, gaps per facility, equity gradient) is a whole-population computation. Nightly batch is cheaper and produces a consistent snapshot everyone quotes the same day.',
    scale: '3M patients in ~15 minutes.',
    alt: 'Stream everything — 100× the cost for numbers that only change materially over weeks.',
    p1: 'stratify_cohort() computes on demand over 4,000 patients.',
    cost: '≈ $30–100/mo.' },
  { id: 'monitor', lane: 3, row: 4, label: 'Drift monitoring', sub: 'BigQuery scheduled drift jobs (JS divergence) → Cloud Monitoring alerts · Model Monitoring v2 where offered', icon: 'monitor',
    why: 'A population changes (new residents, new drugs, a pandemic). Monitoring compares live feature and prediction distributions to the training baseline and opens a retraining ticket before the AUC quietly decays.',
    scale: 'Vertex Model Monitoring v2 is offered only in us-central1, europe-west4, asia-east1 and asia-southeast1 — not Doha. So drift is computed in-region: prediction logs land in BigQuery, a scheduled query compares feature and score distributions to the training baseline (Jensen–Shannon / L∞, same defaults as Model Monitoring), and Cloud Monitoring alerts open the retraining ticket.',
    alt: 'Waiting for clinicians to notice — they will, a year late.',
    p1: 'Subgroup TPR/FPR table on the Evaluation tab; no drift job yet.',
    cost: 'BigQuery scan of the logs ≈ tens of dollars; Model Monitoring would be $3.50/GB analysed if used in a supported region on de-identified logs.' },
  { id: 'looker', lane: 3, row: 5, label: 'Looker · BI Engine', sub: 'ministry dashboards · governed metric definitions', icon: 'looker',
    why: 'Not every question needs an agent. Directors want a stable, governed dashboard; the agent is for the questions the dashboard did not anticipate.',
    scale: 'BI Engine in-memory acceleration on the hot marts.',
    alt: 'Only chat — unnecessary friction for daily KPIs.',
    p1: 'Five React dashboards with 3D charts on the same data.',
    cost: 'Looker licence (enterprise) or Looker Studio Pro; BI Engine ≈ $30/GB-mo reserved.' },

  // ── Agents ──
  { id: 'runtime', lane: 4, row: 0, label: 'ADK runtime — Cloud Run (Doha)', sub: 'supervisor + 5 specialists · sessions in AlloyDB · OTel traces · Agent Engine when offered in Doha', icon: 'agent',
    why: 'Agent Engine is the natural home for this ADK graph — but its region list (us, europe, asia, australia, americas) does not include me-central1. Conversation state carries clinical context, so the runtime stays in Doha: the same ADK tree runs on Cloud Run with DatabaseSessionService on AlloyDB, OpenTelemetry to Cloud Trace, and per-agent service accounts. Moving to Agent Engine is a deployment change, not a code change, the day it launches in Qatar.',
    scale: 'Cloud Run autoscaling on concurrency (≈ 40 concurrent conversations per 4-vCPU instance for async ADK); ≈ 3 tool hops per question.',
    alt: 'Agent Engine in europe-west1 with pseudonymised payloads — defensible under ADR-01, but the ministry would have to accept conversation state outside Qatar. Presented as an option, not the default.',
    p1: 'ADK Runner + InMemorySessionService inside the FastAPI process.',
    cost: 'Cloud Run Doha (tier 2) instance pricing; ≈ $150/mo pilot → ≈ $1.2k/mo national. Agent Engine would be $0.085/vCPU-h + $0.009/GiB-h (idle not billed).' },
  { id: 'gemini', lane: 4, row: 1, label: 'Gemini 3.8 Flash', sub: 'function calling · streaming · 1M context · agent-tuned', icon: 'gemini',
    why: 'Chosen on our own evalset, not on a leaderboard: best trajectory recall per dollar, GA, a year from any deprecation. Pro is reserved for the judge and long syntheses.',
    scale: 'Gemini 3.x is served from the global endpoint (no me-central1 regional endpoint) — acceptable because the prompt carries pseudonymous ids and aggregates, never identifiers (ADR-01). Dynamic shared quota by default; Provisioned Throughput (≈ $2.7k per GSU-month) above ~100k q/day. thinking_level LOW on routing turns keeps first-token latency under a second.',
    alt: 'Gemini 3.1 Pro everywhere — 3× the cost for no measurable gain on tool-structured tasks. Gemini 2.5 Flash — retiring Oct 2026.',
    p1: 'Same model via the Gemini API; resolver falls back to 3.5 Flash.',
    cost: '$0.75 in / $3.75 out per 1M tokens (intro to Dec 2026, then $1.50 / $7.50); cached input $0.075. ≈ $3.8k/mo at 20k single-turn questions/day; long multi-turn sessions raise it, context caching of the system prompt and schemas pulls it back.' },
  { id: 'mcp', lane: 4, row: 2, label: 'MCP layer', sub: 'MCP Toolbox (FHIR · BigQuery) + Nabd pop-health MCP server (Cloud Run)', icon: 'mcp',
    why: 'Google gives agents managed MCP servers to read FHIR and query BigQuery — but none that reasons about a population. The pop-health server (care gaps, stratification, benchmarking, simulation) is the gap Nabd fills, and any MCP-speaking agent can use it.',
    scale: 'Stateless Cloud Run service, streamable HTTP, scale-to-zero.',
    alt: 'Plain Python tools inside the agent — locks the capability to one runtime; MCP makes it a governed, versioned, reusable product.',
    p1: 'pophealth_mcp over stdio as a subprocess of the ADK process (7 tools).',
    cost: 'Cloud Run ≈ $50–300/mo.' },
  { id: 'armor', lane: 4, row: 3, label: 'Prompt & response screening', sub: 'Sensitive Data Protection in-region · Model Armor (nearest region) for injection / jailbreak', icon: 'armor',
    why: 'Prompts and answers pass through a policy layer before the model or the user sees them. Model Armor\'s Vertex integration is only in four US/EU regions, so the order is: Sensitive Data Protection de-identifies any pasted identifier inside me-central1 first, then the sanitised prompt is screened for injection and jailbreak by Model Armor, then it reaches Gemini.',
    scale: 'Managed; per-request latency in the tens of ms.',
    alt: 'Regex in the app — brittle.',
    p1: 'System-instruction rules + numbers-only-from-tools policy; no external screen.',
    cost: 'Model Armor $0.10 per 1M tokens after 2M free; SDP per GB inspected. Tens to a few hundred dollars per month.' },
  { id: 'eval', lane: 4, row: 4, label: 'Gen AI Evaluation + adk eval', sub: 'tool_trajectory_avg_score · hallucinations_v1 · safety_v1 · rubric judges · CI gate', icon: 'eval',
    why: 'The evalset on the Evaluation tab runs on every PR (Cloud Build) and weekly on sampled production traffic. Models and prompts change; the gate does not.',
    scale: 'Offline; 10–200 cases per run.',
    alt: 'Vibes.',
    p1: 'Local harness: trajectory recall, groundedness, action safety, numeric faithfulness, cost.',
    cost: 'Computation-based metrics $0.00003 per 1k characters; model-based metrics consume judge-model tokens; ≈ $20–100/mo.' },

  // ── Experience ──
  { id: 'iap', lane: 5, row: 0, label: 'SSO · IAP · Cloud Armor', sub: 'MOPH SSO (SAML/OIDC) · WAF · rate limits · geo-fence', icon: 'lock',
    why: 'Clinicians sign in with the hospital identity; roles (clinician vs ministry analyst) decide which tools the agent may call on their behalf. Cloud Armor keeps the app Qatar-only.',
    scale: 'Managed.',
    alt: 'App-level passwords — never for PHI.',
    p1: 'Two personas switch roles in the header; role passed to the agent context.',
    cost: '≈ $100–600/mo incl. WAF rules.' },
  { id: 'app', lane: 5, row: 1, label: 'Cloud Run — Nabd web + API', sub: 'React · FastAPI · NDJSON streaming · same container as today', icon: 'run',
    why: 'The phase-1 container is already the phase-2 artefact: one Dockerfile, health check, stateless. Cloud Run gives HTTPS, autoscaling, revisions and traffic splitting for canaries.',
    scale: 'Scale 1 → N instances on concurrency; min 1 to avoid cold starts for clinicians.',
    alt: 'GKE — more control than this app needs.',
    p1: 'Railway runs the identical container.',
    cost: 'Doha tier-2 pricing ($0.0000216/vCPU-s); ≈ $80/mo pilot → ≈ $600/mo national.' },
  { id: 'speech', lane: 5, row: 2, label: 'Speech-to-Text · Text-to-Speech', sub: 'Chirp 3 · Arabic (ar-QA) + English · streaming', icon: 'mic',
    why: 'Voice is how a clinician asks between patients and how a director asks from a car. Arabic quality is the make-or-break; Chirp 3 handles Gulf Arabic and code-switching.',
    scale: 'Streaming recognition per session.',
    alt: 'Browser Web Speech — free, uneven Arabic quality, no audit copy of the transcript.',
    p1: 'Web Speech API (EN/AR) + speechSynthesis in the browser.',
    cost: 'Per audio minute; ≈ $40–500/mo.' },
  { id: 'queue', lane: 5, row: 3, label: 'HITL queue → EHR write-back', sub: 'approved drafts → FHIR Task / ServiceRequest / CommunicationRequest', icon: 'queue',
    why: 'The agent never writes to a record. It drafts; a named clinician approves; the approved item is posted to the FHIR store as a Task the EHR picks up. The audit trail links draft → decision → resource id.',
    scale: 'Hundreds of items/day.',
    alt: 'Autonomous prescribing — not in this decade, not in Qatar, not by us.',
    p1: 'Queue tab with approve/reject, audit events; no external write.',
    cost: 'Included in the FHIR store request pricing.' },
  { id: 'users', lane: 5, row: 4, label: 'Clinicians · MOPH directors', sub: 'browser · tablet · voice · Looker', icon: 'users',
    why: 'Two personas, two questions: "who on my panel needs me today?" and "where is the system failing and what would fixing it cost?"',
    scale: '~2,000 clinicians, ~50 analysts; 20k questions/day at national scale is the planning number.',
    alt: '—', p1: 'Dr. Amal (clinician) and Dr. Khalid (ministry) personas.', cost: '—' },
];

/* Edge kinds: stream (event-driven, seconds) · batch (scheduled) · request (synchronous, user-facing) · control (governance / MLOps) */
export const EDGES = [
  ['qhie', 'fhirstore', 'stream'], ['ehr', 'mllp', 'stream'], ['mllp', 'fhirstore', 'stream'],
  ['fhirstore', 'pubsub', 'stream'], ['fhirstore', 'bq', 'stream'], ['pubsub', 'endpoint', 'stream'],
  ['endpoint', 'queue', 'stream'], ['fs', 'endpoint', 'stream'],
  ['claims', 'batchland', 'batch'], ['batchland', 'bq', 'batch'], ['guidelines', 'docai', 'batch'], ['docai', 'vector', 'batch'],
  ['bq', 'train', 'batch'], ['train', 'registry', 'batch'], ['registry', 'endpoint', 'batch'], ['registry', 'batchpred', 'batch'],
  ['bq', 'batchpred', 'batch'], ['batchpred', 'bq', 'batch'], ['bq', 'fs', 'batch'], ['bq', 'looker', 'batch'], ['bq', 'deid', 'batch'],
  ['users', 'iap', 'request'], ['iap', 'app', 'request'], ['app', 'armor', 'request'], ['armor', 'runtime', 'request'],
  ['runtime', 'gemini', 'request'], ['runtime', 'mcp', 'request'], ['mcp', 'bq', 'request'], ['mcp', 'endpoint', 'request'],
  ['mcp', 'vector', 'request'], ['app', 'alloydb', 'request'], ['app', 'speech', 'request'], ['app', 'queue', 'request'], ['queue', 'fhirstore', 'request'],
  ['monitor', 'train', 'control'], ['eval', 'runtime', 'control'], ['catalog', 'bq', 'control'], ['endpoint', 'monitor', 'control'],
];

export const EDGE_STYLE = {
  stream: { color: '#9b1c46', dash: '', label: 'Real-time event (seconds)', width: 2 },
  batch: { color: '#b8862e', dash: '7 5', label: 'Scheduled batch (nightly / weekly)', width: 1.8 },
  request: { color: '#6d4fa8', dash: '', label: 'Synchronous request (user-facing)', width: 1.6 },
  control: { color: '#64748b', dash: '2 4', label: 'Control plane (MLOps · governance)', width: 1.4 },
};

/* Named flows the presenter can light up one at a time */
export const FLOWS = [
  { id: 'all', label: 'Everything', nodes: null, edges: null },
  { id: 'realtime', label: 'Patient event → risk → alert', kind: 'stream',
    nodes: ['qhie', 'ehr', 'mllp', 'fhirstore', 'pubsub', 'fs', 'endpoint', 'queue', 'bq'],
    budget: 'p95 < 5 s from result posted to alert queued',
    story: 'A lab posts an HbA1c of 9.6 at Al Wakrah. The FHIR store validates it and emits a notification; the scorer pulls the patient\'s features, calls the endpoint, and the risk climbs from 31% to 44%. The care-gap engine queues a statin recall for the nurse. The same event has already streamed to BigQuery, so the dashboard shows it too.' },
  { id: 'batch', label: 'Nightly cohort refresh', kind: 'batch',
    nodes: ['claims', 'batchland', 'bq', 'deid', 'registry', 'batchpred', 'looker', 'fs'],
    budget: '02:00–03:00 AST · complete before the 07:00 huddles',
    story: 'Claims land, the pipeline validates and de-identifies, the whole population is re-scored with the approved model version, quality measures and the equity gradient are recomputed, features are refreshed, and Looker caches warm. Everyone quotes the same snapshot all day.' },
  { id: 'request', label: 'Clinician asks a question', kind: 'request',
    nodes: ['users', 'iap', 'app', 'speech', 'armor', 'runtime', 'gemini', 'mcp', 'bq', 'endpoint', 'vector', 'alloydb', 'queue'],
    budget: 'first token < 1.5 s · full answer p95 < 8 s · ≈ 0.6¢ per question',
    story: 'Dr. Amal asks for her morning briefing. IAP checks her SSO, Model Armor screens the prompt, the supervisor plans, three specialists call MCP tools against BigQuery, the endpoint and RAG Engine, Gemini writes the answer with citations, drafts go to the queue and the whole trace is stored.' },
  { id: 'control', label: 'Drift → retrain → release', kind: 'control',
    nodes: ['endpoint', 'monitor', 'train', 'registry', 'eval', 'runtime', 'catalog', 'bq'],
    budget: 'weekly · drift alert to promoted model < 48 h with human approval',
    story: 'Monitoring flags HbA1c-distribution drift after a new lab analyser rolls out. The pipeline retrains, the held-out and fairness gates pass, a reviewer approves in the Registry, the endpoint canaries 10% of traffic, and adk eval re-certifies the agent against the new model before promotion.' },
];

export const DATA_FLOWS = [
  { flow: 'FHIR resource updates (QHIE)', mode: 'Real-time', transport: 'FHIR subscription → Cloud Healthcare API → Pub/Sub', volume: '≈ 500k/day · peak 20/s', latency: 'seconds', why: 'Risk changes when results arrive; clinicians act same-day.' },
  { flow: 'HL7v2 from legacy facilities', mode: 'Real-time', transport: 'MLLP adapter (GKE) → HL7v2 store → Dataflow (Whistle) → FHIR', volume: '≈ 40 msg/s peak', latency: '< 30 s', why: 'Bridges the facilities that need the population view most.' },
  { flow: 'FHIR → BigQuery analytics', mode: 'Streaming export', transport: 'Healthcare API streaming to BigQuery (ANALYTICS_V2 schema, partitioned)', volume: 'same as above', latency: '"near real time" — no published SLA; seconds in practice', why: 'Dashboards and cohort tools read near-live data without a pipeline to operate.' },
  { flow: 'Individual risk scoring', mode: 'Real-time', transport: 'Pub/Sub → Cloud Run scorer → Feature Store → Vertex endpoint', volume: '≈ 500k/day', latency: 'p50 < 10 ms model · < 5 s end-to-end', why: 'Per-patient, cheap, and the alert has a same-day consumer.' },
  { flow: 'Whole-cohort stratification, care gaps, quality measures', mode: 'Nightly batch', transport: 'Vertex batch prediction + BigQuery SQL (scheduled queries)', volume: '3M patients', latency: '≈ 15 min · done by 03:00', why: 'Population metrics move over weeks; one daily snapshot keeps everyone on the same numbers.' },
  { flow: 'Claims · pharmacy · registry files', mode: 'Nightly batch', transport: 'SFTP/Transfer Service → Cloud Storage → Dataflow batch → BigQuery', volume: '≈ 1.5M rows/night', latency: '≈ 6 min', why: 'Source produces them nightly; consumer is a monthly cost report.' },
  { flow: 'Guideline corpus', mode: 'Event-driven batch', transport: 'Bucket upload → Cloud Run job → RAG Engine corpus version', volume: 'quarterly', latency: 'minutes', why: 'Versioned citations; never re-index by hand.' },
  { flow: 'Clinician / ministry question', mode: 'Synchronous', transport: 'Cloud Run → Model Armor → Agent Engine → MCP tools → Gemini (streamed)', volume: '20k/day planning · 1k/day pilot', latency: 'first token < 1.5 s · p95 < 8 s', why: 'Interactive; every answer traceable to tool calls.' },
  { flow: 'Retraining', mode: 'Scheduled + triggered', transport: 'BigQuery drift job alert → Vertex Pipelines → Registry → canary', volume: 'weekly', latency: '< 48 h to promotion', why: 'Populations drift; the AUC must not decay silently.' },
];

export const SCALE = {
  assumptions: [
    ['Population', '3.0M people · ≈ 400k with diabetes or CVD risk in scope'],
    ['Clinical events', '≈ 500k FHIR resource updates/day · 20/s peak · 2 TB/year in BigQuery'],
    ['Users', '≈ 2,000 clinicians · ≈ 50 ministry analysts'],
    ['Questions', '1k/day pilot (4 sites) → 20k/day national'],
    ['Per question', '≈ 3 tool hops · ≈ 3.9k prompt tokens · ≈ 900 output tokens · ≈ 0.6¢'],
    ['Availability', '99.9% for the app and scorer (3 zones in me-central1); RPO 15 min, RTO 1 h within region — cross-region DR would breach the Qatar boundary, so resilience is multi-zone plus tested restores'],
  ],
  inference: [
    ['Risk model', 'XGBoost 300 trees · depth 4 · 30 features', '≈ 1 ms CPU per score · p50 < 10 ms at the endpoint', 'Vertex online endpoint, 2 min replicas (HA), autoscale on CPU; batch prediction for the nightly full re-score'],
    ['Explanations', 'pred_contribs (TreeSHAP) per score', '+1 ms', 'Returned with the score so the clinician sees drivers, not a number'],
    ['LLM', 'Gemini 3.8 Flash (global endpoint)', 'first token ≈ 0.8 s at thinking_level LOW · ≈ 250 tok/s streaming', 'Dynamic shared quota → Provisioned Throughput (≈ $2.7k / GSU-month) at national scale; Pro for judge only'],
    ['Retrieval', 'RAG Engine (hybrid) top-6 chunks', '< 300 ms', 'Corpus small; latency dominated by the LLM turn'],
    ['Voice', 'Chirp 3 streaming STT · TTS', 'partial transcripts < 500 ms', 'Arabic + English; transcripts audited'],
  ],
  scaling: [
    ['Ingest', 'Pub/Sub + Dataflow streaming autoscale on backlog; FHIR store is managed. No capacity planning beyond quotas.'],
    ['Analytics', 'BigQuery serverless; partition by event date, cluster by patient; BI Engine for hot dashboards; slot reservation if spend > $2k/mo.'],
    ['Scoring', 'Stateless endpoint replicas; 1 replica ≈ 50× national peak. Scale is not the problem — versioning and monitoring are.'],
    ['Agents', 'ADK on Cloud Run in Doha, autoscaled on concurrency; ≈ 3 LLM calls/question; sessions in AlloyDB. Token budget is the real scaling variable — hence the specialist topology (Evaluation → LLM & cost) and context caching of schemas.'],
    ['App', 'Cloud Run min 1 / max 20 instances, 80 concurrent requests each; NDJSON streaming keeps connections short.'],
    ['State', 'AlloyDB primary + read pool; Agent Engine Sessions/Memory Bank for conversation state; nothing in the container.'],
  ],
};

export const ADRS = [
  { id: 'ADR-01', title: 'PHI stays in Doha; the LLM sees pseudonymous, aggregated data',
    decision: 'All stores (FHIR, BigQuery, AlloyDB, Feature Store) in me-central1 under an Assured Workloads Qatar Data Boundary folder, CMEK, VPC Service Controls. Agents and Gemini receive only the pseudonymised analytics zone: stable QH-ids, no names or national ids, cohort aggregates. Re-identification is an app-layer join inside the perimeter for an authorised role.',
    because: 'PDPPL treats health data as sensitive; MOPH will not accept a design whose privacy depends on a prompt. Separating the identity zone from the reasoning zone makes the LLM\'s region a latency question, not a residency question.',
    consequences: 'Gemini endpoint region can be chosen for latency/availability; every re-identification is an audited event; DR must stay in-region (cross-region replication would breach the boundary) — accepted, with multi-zone HA and tested restores.' },
  { id: 'ADR-02', title: 'Cloud Healthcare API FHIR store as the landing zone, not raw BigQuery',
    decision: 'Mirror QHIE into a managed FHIR R4 store; stream to BigQuery from there.',
    because: 'Validation, resource versioning, consent enforcement, Pub/Sub notifications and a native streaming export come for free. BigQuery-only ingestion re-implements all of that badly.',
    consequences: 'The Healthcare API is offered in me-central1, so the store sits in Doha. Consent enforcement only takes effect after applyConsents runs, so consent changes trigger a re-apply job. Bulk history is loaded once by batch export (imports do not stream).' },
  { id: 'ADR-03', title: 'Hybrid real-time / batch — event-driven per-patient, nightly per-population',
    decision: 'Individual risk re-scored on each new result (seconds). Cohort stratification, care gaps, quality measures and the equity gradient recomputed nightly.',
    because: 'A clinician needs today\'s risk for the patient in front of them; a director needs one consistent national snapshot. Streaming population aggregates would cost ~100× for numbers that change over weeks.',
    consequences: 'Dashboards carry an as-of timestamp; the agent states which numbers are live and which are the nightly snapshot.' },
  { id: 'ADR-04', title: 'Classical ML scores; the LLM explains — never the reverse',
    decision: 'Risk is a calibrated XGBoost model with held-out evaluation, fairness gates and TreeSHAP explanations. Gemini narrates, plans tool calls and drafts; it never produces a probability.',
    because: 'Risk scores must be calibrated, auditable, cheap and stable across model updates. LLM-generated risk is none of those. Every number in an answer is re-computed by a tool (Evaluation → faithfulness).',
    consequences: 'Two lifecycles: model versions in the Registry, prompt/agent versions in the evalset gate. MedGemma is a roadmap item for unstructured notes only.' },
  { id: 'ADR-05', title: 'Supervisor + specialists (ADK) over one flat agent',
    decision: 'One supervisor routes to five specialists (cohort, guidelines, risk/ML, pop-health MCP, actions), each with its own tools and instruction.',
    because: 'Measured: a flat agent re-reads ~2.5k schema tokens per hop; the supervisor reads ~0.7k and specialists only their own — about 30% cheaper per question at scale, and since Gemini 3.5 function declarations are billed as input tokens, that arithmetic is literally the invoice. More important: least privilege (the guideline agent cannot draft a prescription), independent evaluation, and the MCP agent is swappable for Google\'s managed servers without touching the rest.',
    consequences: 'One extra LLM hop of latency (~0.7 s); mitigated by streaming the trace so the user sees progress.' },
  { id: 'ADR-06', title: 'Gemini 3.8 Flash by default; Pro only as judge and for long syntheses',
    decision: 'Flash 3.8 for all agents; 3.1 Pro for LLM-as-judge in evaluation and monthly reports. 2.5 Flash excluded (retiring Oct 2026).',
    because: 'Chosen on our evalset: equal trajectory recall to Pro on tool-structured tasks at a third of the cost and half the latency; GA; a year from deprecation. Intro pricing ends Dec 2026 — the 2027 budget is already on the LLM tab.',
    consequences: 'Version pinning and re-evaluation on every model update; Arabic-fidelity judge scores decide whether voice turns ever route to Pro. Gemini 3.x is global-endpoint only, which ADR-01 already makes acceptable.' },
  { id: 'ADR-07', title: 'Build the population-health MCP server; reuse Google\'s MCP servers for FHIR and BigQuery',
    decision: 'Nabd\'s pop-health tools (care gaps, stratification, benchmarking, equity, simulation, quality measures) are exposed as an MCP server. Data access goes through Google\'s MCP Toolbox servers.',
    because: 'Google\'s catalogue covers reading FHIR and querying BigQuery — not reasoning about a population. Publishing ours as MCP makes it reusable by any agent runtime the ministry adopts, versioned, and governable at the tool boundary.',
    consequences: 'An extra network hop per tool call (~20 ms on Cloud Run); the server needs its own auth (IAM + per-agent identity).' },
  { id: 'ADR-08', title: 'No autonomous clinical writes — HITL queue then FHIR Task',
    decision: 'Every draft (prescription, recall, referral) lands in a queue; a named clinician approves; only then is a FHIR Task/ServiceRequest posted for the EHR to act on.',
    because: 'Clinical accountability and MOPH regulation. Also the cheapest possible safety mechanism: a human at the only point where harm could occur.',
    consequences: 'Throughput bounded by reviewers; mitigated by prioritising the queue with the same risk model.' },
  { id: 'ADR-09', title: 'Managed retrieval (RAG Engine) with page-level citations',
    decision: 'Phase-2 guideline retrieval on Vertex AI RAG Engine with a versioned corpus; citations must resolve to document + page.',
    because: 'The groundedness gate requires citations that a clinician can open. Managed corpus versioning prevents citation rot. Agent Search for healthcare is being retired, so the general RAG Engine is the durable choice.',
    consequences: 'Answers without a resolvable citation fail evaluation; the phase-1 hybrid index keeps the exact same citation shape so the swap is invisible to the agent.' },
  { id: 'ADR-10', title: 'ADK on Cloud Run in Doha today; Agent Engine when it reaches me-central1',
    decision: 'The ADK graph runs on Cloud Run in me-central1 with DatabaseSessionService on AlloyDB, OpenTelemetry to Cloud Trace and per-agent service accounts. The pop-health MCP server and the web/API container are separate Cloud Run services.',
    because: 'Agent Engine is the better managed runtime (sessions, Memory Bank, identity, tracing), but its region list does not include Qatar, and conversation state carries clinical context that MOPH will want in-country. Cloud Run is in Doha, is the phase-1 container already, and keeps the code identical.',
    consequences: 'We operate session storage and tracing ourselves (a few hundred lines, already written for phase 1). Migration to Agent Engine is a deploy target change. Same trade-off applies to Model Armor and Model Monitoring — both replaced with in-region equivalents (SDP + BigQuery drift jobs) with the managed services listed as the upgrade path.' },
];

export const PHASES = [
  ['Data', 'Synthetic HIE in pandas (8 tables, 36 months, FHIR-shaped export)', 'QHIE FHIR subscription → Cloud Healthcare API → BigQuery streaming export; HL7v2 via MLLP adapter', 'Same table contract; hie.py becomes BigQuery SQL / MCP Toolbox'],
  ['Consent & privacy', 'Tool-layer consent check; pseudonymous ids; audited denials', 'FHIR consent enforcement; Healthcare API de-identification; Assured Workloads Qatar boundary; CMEK; VPC-SC', 'Policy moves from code to platform'],
  ['Risk model', 'XGBoost trained at container build; scored in-process; TreeSHAP', 'Vertex Pipelines → Registry → online endpoint (Doha) + nightly BigQuery batch scoring; Feature Store; BigQuery drift jobs', 'Same features, same model family, same evaluation tab'],
  ['Retrieval', 'BM25 + gemini-embedding-001 hybrid, cached; page citations', 'Vertex AI RAG Engine, versioned corpus, grounding metadata', 'Citation shape unchanged'],
  ['Agents', 'ADK Runner inside FastAPI; InMemorySessionService', 'ADK on Cloud Run (Doha) with DatabaseSessionService → AlloyDB, OTel → Cloud Trace, SDP + Model Armor screening; Agent Engine when offered in me-central1', 'The LlmAgent tree deploys as-is'],
  ['MCP', 'pophealth_mcp over stdio (7 tools)', 'Cloud Run streamable-HTTP MCP server + Google MCP Toolbox (FHIR, BigQuery)', 'Same tool contracts'],
  ['Queue & audit', 'JSON files; Audit tab', 'AlloyDB; Cloud Audit Logs; FHIR Task write-back after approval', 'Same events, durable store'],
  ['Identity', 'Two personas in the header', 'Identity Platform (MOPH SSO) + IAP + Cloud Armor; role → tool allow-list', 'Roles already flow into the agent context'],
  ['Voice', 'Browser Web Speech (EN/AR) + speechSynthesis', 'Speech-to-Text / Text-to-Speech (Chirp 3, ar-QA) with audited transcripts', 'UI unchanged'],
  ['Evaluation', 'Local harness: trajectory, groundedness, safety, faithfulness, cost', 'adk eval in Cloud Build as a release gate + Gen AI Evaluation Service judge + weekly production sampling', 'Evalset file is already ADK-compatible'],
  ['Hosting', 'Railway, single container', 'Cloud Run (app, MCP) + Agent Engine (agents); Cloud Build CI/CD; Terraform', 'Same Dockerfile'],
];

/* Order-of-magnitude monthly run cost at list prices (USD, Sept 2026). Pilot = 4 sites, 1k questions/day. National = 18 facilities, 20k/day. */
export const COST = [
  ['Gemini 3.8 Flash inference', 190, 3760, 'Measured token budget × list price; +100% from Jan 2027 at standard pricing'],
  ['ADK runtime on Cloud Run (Doha)', 150, 1200, 'Tier-2 instance pricing; Agent Engine would be $0.085/vCPU-h when available in Qatar'],
  ['Cloud Healthcare API (FHIR + HL7v2 stores)', 120, 700, 'Doha: $0.292/GiB-mo structured, $0.39/100k requests, $0.29/1M notifications, streaming export per GiB'],
  ['BigQuery (storage + queries + BI Engine)', 120, 1000, 'Doha: $6.25/TiB, $0.026/GiB-mo; slot reservation above ~$2k'],
  ['Pub/Sub + Dataflow (streaming + batch)', 150, 1200, 'Autoscaled streaming job is the main line'],
  ['Vertex online endpoint (risk, 2–4 replicas)', 275, 550, 'Doha e2-standard-4 $0.187/h; 24/7 for HA'],
  ['BigQuery ML batch scoring + Pipelines + drift jobs', 40, 250, '$0.03/pipeline run + compute; scoring 3M rows is a few TiB-scans a month'],
  ['Feature Store online serving (Bigtable)', 0, 830, 'Pilot reads features from BigQuery; one Doha Bigtable node at national scale'],
  ['RAG Engine (RagManagedDb basic) + embeddings', 65, 130, 'Basic tier ≈ $65/mo; embeddings $0.15/1M tokens'],
  ['Cloud Run (web/API + MCP server)', 80, 600, 'min-instances 1 for the app'],
  ['AlloyDB (queue, audit)', 250, 900, 'Basic → HA'],
  ['Speech-to-Text / Text-to-Speech', 40, 500, 'Per audio minute'],
  ['Security (IAP, Cloud Armor, SDP, Model Armor, VPC-SC, CMEK)', 100, 600, 'Model Armor $0.10/1M tokens after 2M free'],
  ['Logging · Trace · Monitoring', 50, 400, ''],
];
export const AVOIDED_ADMISSION_USD = 8800;   // QAR 32,000 mean complication admission (HMC costing) ≈ USD 8,800
